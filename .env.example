# Frontend environment example for Gyaano
# Copy to `.env` at project root (or set via your hosting provider) and restart dev server.

# Google OAuth Client ID for Sign in with Google
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# API base URL for your backend (production)
VITE_API_BASE_URL=https://quizify.duckdns.org

# Socket server URL (if different from API_BASE_URL)
VITE_SOCKET_URL=https://quizify.duckdns.org
